#!/usr/bin/perl

use strict;
use warnings;

use FindBin;
use lib "$FindBin::Bin/lib";
use Bootstrapper;

# Check if Mojo is installed
use Mojolicious;

# Start application
use CometDesktop;
CometDesktop->start;
